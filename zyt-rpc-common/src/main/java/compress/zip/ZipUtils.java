package compress.zip;

import compress.Compress;

public class ZipUtils implements Compress {
    @Override
    public byte[] compress(byte[] bytes) {
        return new byte[0];
    }

    @Override
    public byte[] deCompress(byte[] bytes) {
        return new byte[0];
    }
}
