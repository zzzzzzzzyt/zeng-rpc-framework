package compress.sevenZ;

import compress.Compress;

public class SevenZUtils implements Compress {
    @Override
    public byte[] compress(byte[] bytes) {
        return new byte[0];
    }

    @Override
    public byte[] deCompress(byte[] bytes) {
        return new byte[0];
    }
}
